---
title: 'Intento # 2'
author: "Miguel Ángel Londoño Ciceros"
date: "27/5/2021"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

# Introducción

* Lectura de la base de datos

```{r}
df <- read.csv("base_trabajo_segmentacion.csv", sep = ";", dec = '.', header = T)
df <- df[, -1]
head(df)
```

```{r}
colnames(df)
```


* Separación de las variables financieras y canales (no escaladas)

```{r}
finan_no_scaled <- df[, 31:46]
Canal_no_scaled <- df[ , 1:30]
```

* Escalamiento de los datos completos

```{r}
df_scaled <- scale(df, center = T, scale = T)
```


* Separando en conjunto de datos de canales y financiera donde ya estan escaladas:

```{r}
Canal_scaled <- df_scaled[,1:30]
finan_scaled <- df_scaled[,31:46]
```

## Análisis descriptivo de las variables financieras

```{r}
library(ggplot2)
for(i in 1:dim(finan_no_scaled)[2]){
  
  if(class(finan_no_scaled[, i])=="integer"){
    p <- ggplot(finan_no_scaled, aes(x = finan_no_scaled[, i], y = ..prop..))+ geom_bar(fill = "#0c4c8a")
  }else{
    p <- ggplot(finan_no_scaled, aes(x=finan_no_scaled[, i])) +  geom_histogram(bins = 10, fill = "#0c4c8a")
  }
  
  p_final <- p +
    labs(x = colnames(finan_no_scaled)[i]) +
    theme_gray() 
    
  
  print(p_final)
}
```

Interpretaciones de las varibles solas:

 * La mayoría de las empresas presentan un nivel bajo de exportaciones

Interpretaciones comparandolas con los grupos:

```{r}
pairs(finan_no_scaled[,c(6,7,10,11)])
```



### Matriz de correlaciones


Correlaciones entre las variables financieras

```{r}
library(corrplot)
corrplot(cor(finan_no_scaled), 
         method="color",  
         type="upper", order="hclust", 
         addCoef.col = "black", # Add coefficient of correlation
         tl.col="black", tl.srt=45, #Text label color and rotation
         sig.level = 0.01, insig = "blank", 
         number.cex = 0.6,
         # hide correlation coefficient on the principal diagonal
         diag=FALSE 
         )

```


Análisis descriptivo de los canales

Matriz de correlaciones de las variables de los canales


```{r}
library(corrplot)
corrplot(cor(Canal_no_scaled), 
         method="color",  
         type="upper", order='original', 
         addCoef.col = "black", # Add coefficient of correlation
         tl.col="black", tl.srt=45, #Text label color and rotation
         sig.level = 0.01, insig = "blank", 
         number.cex = 0.5,
         # hide correlation coefficient on the principal diagonal
         diag=FALSE
         )
```

Variables más correlacionadas

```{r}
library(corrr)
library(dplyr)
corr_canales <- Canal_no_scaled %>%
    correlate() %>% 
    stretch() %>% 
    arrange(r)

corr_altas <- corr_canales[which(corr_canales$r > 0.9), ]

nombres <- c(corr_altas$x, corr_altas$y)
unique(nombres)
```


matriz de dispersión entre las variables más correlacionadas

```{r}
pairs(log(Canal_no_scaled[,unique(nombres)]+1))
```


Gráficos descriptivos de los canales

```{r}
for(i in 1:30){
  plot(density(df[,i]),xlim=c(0,10000000000))
}

```


```{r}
par(mfrow=c(1,2))
boxplot(df[,1:5], las=2, ylim=c(0, 1000000000))
boxplot(df[,6:11], las=2, ylim=c(0, 1000000000))
```


Boxplots con los canales

```{r}
par(mfrow=c(2,3))
boxplot(df[,1:5], las=2)
boxplot(df[,6:10], las=2)
boxplot(df[,11:15], las=2)
boxplot(df[,16:20], las=2)
boxplot(df[,21:25], las=2)
boxplot(df[,26:30], las=2)
```



Aqui termina el análisis descriptivo 
#######################################################################################


* Se utiliza agrupamiento jerarquico con la idea de que nos diga la cantidad de grupos que se deben considerar 

```{r}
library(ggdendro)
library(ggplot2)
require(factoextra)

dendrogram <- hclust(dist(finan_scaled, method = 'euclidean'), method = 'ward.D')


plot(dendrogram, xlab="", sub="", cex=0.9)
rect.hclust(dendrogram, k=3, border="cyan4")
```


* Agrupamiento k means con las variables financieras:

```{r}
require(cluster)

set.seed(93284)
Agru_finan <- kmeans(finan_scaled, 3 , nstart = 5, iter.max = 100)

clusplot(finan_scaled, Agru_finan$cluster, color = TRUE, 
          shade = FALSE, labels = 4, lines = 1,
          col.p = Agru_finan$cluster )
```


```{r}
table(Agru_finan$cluster)
```


* Creación de un conjunto de datos con las variables financieras y la variable grupo 

```{r}
grupos <- as.factor(Agru_finan$cluster)

fina_group <- data.frame(grupos, finan_no_scaled)
head(fina_group)
```

* Niveles de la variable grupo

```{r}
levels(fina_group$grupos)
```

* Gráficos de barras que nos permitiran sacar carateristicas de cada uno de los grupos

```{r}
library(ggplot2)
for(i in 2:dim(fina_group)[2]){
  
  if(class(fina_group[, i])=="integer"){
    p <- ggplot(fina_group, aes(x = fina_group[, i], y = ..prop..))+ geom_bar(fill = "#0c4c8a")+facet_grid(vars(), vars(grupos))
  }else{
    p <- ggplot(fina_group, aes(x = factor(grupos), y=fina_group[, i])) +  geom_boxplot()
  }
  
  p_final <- p +
    labs(x = colnames(fina_group)[i]) +
    theme_gray() 
    
  
  print(p_final)
}

```


```{r}
library(ggplot2)
for(i in 2:dim(fina_group)[2]){
  
  if(class(fina_group[, i])=="integer"){
    p <- ggplot(fina_group, aes(x = fina_group[, i], y = ..prop..))+ geom_bar(fill = "#0c4c8a")
  }else{
    p <- ggplot(fina_group, aes(x = fina_group[, i])) +  geom_histogram(bins = 10, fill = "#0c4c8a")
  }
  
  p_final <- p +
    labs(x = colnames(fina_group)[i]) +
    theme_gray() + 
    facet_grid(vars(), vars(grupos))
  
  print(p_final)
}
```


* Grupos de las variables escaladas 

```{r}
df_scaled_group <- data.frame(grupos,df_scaled)
#outliers <- c(825, 1416, 1462, 1668, 1773) # Esto va a ser un solo grupo posteriormente
#df_scaled_group <- df_scaled_group[-outliers,]
```


```{r}
grupo1_scaled <- df_scaled_group[df_scaled_group$grupos==1,]
grupo2_scaled <- df_scaled_group[df_scaled_group$grupos==2,]
grupo3_scaled <- df_scaled_group[df_scaled_group$grupos==3,]
```



* Grupos de las variables no escaladas 

```{r}
df_group <- data.frame(grupos,df)
#outliers <- c(825, 1416, 1462, 1668, 1773) # Esto va a ser un solo grupo posteriormente
#df_group <- df_group[-outliers,]
```


```{r}
grupo1_no_scaled <- df_group[df_group$grupos==1,]
grupo2_no_scaled <- df_group[df_group$grupos==2,]
grupo3_no_scaled <- df_group[df_group$grupos==3,]
```


* Agrupamiento por canales en el grupo 1 de financieras:

```{r}
dendrogram <- hclust(dist(log(grupo1_no_scaled[,2:31]+1), method = 'euclidean'), method = 'ward.D')

plot(dendrogram, xlab="", sub="", cex=0.9)
```




```{r}
require(cluster)


set.seed(93284)
kc1 <- kmeans(log(grupo1_no_scaled[,2:31]+1), 3 , nstart = 5, iter.max = 100)

clusplot(log(grupo1_no_scaled[,2:31]+1), kc1$cluster, color = TRUE, 
          shade = FALSE, labels = 4, lines = 1,
          col.p = kc1$cluster )
```


```{r}
table(kc1$cluster)

```


Creación del conjunto de datos con los grupos creados:


```{r}
finan1_canal <- data.frame(sub_grupos=kc1$cluster, grupo1_no_scaled)
finan1_canal$sub_grupos <- as.factor(finan1_canal$sub_grupos)
head(finan1_canal)
```




```{r}
library(ggplot2)
for(i in 3:dim(finan1_canal)[2]){
  
  p <- ggplot(finan1_canal, aes(x = factor(sub_grupos), y=log(finan1_canal[, i]+1))) +  geom_boxplot()+
    labs(x = colnames(finan1_canal)[i]) +
    theme_gray() 
    
  
  print(p)
}

```

* Media de los grupos

```{r}
aggregate(.~sub_grupos,data=finan1_canal, FUN=mean)
```


* Mediana de los grupos

```{r}
aggregate(.~sub_grupos,data=finan1_canal, FUN=median)
```



 * Agrupamiento por canales en el grupo 2 de financieras:

```{r}
dendrogram <- hclust(dist(log(grupo2_no_scaled[,2:31]+1), method = 'euclidean'), method = 'ward.D')

plot(dendrogram, xlab="", sub="", cex=0.9)
```


```{r}
require(cluster)

set.seed(93284)
kc2 <- kmeans(log(grupo2_no_scaled[,2:31]+1), 3, nstart = 5, iter.max = 100)

clusplot(log(grupo2_no_scaled[,2:31]+1), kc2$cluster, color = TRUE, 
          shade = FALSE, labels = 4, lines = 1,
          col.p = kc2$cluster )
```


```{r}
table(kc2$cluster)
```


Creación del conjunto de datos con los grupos creados:


```{r}
finan2_canal <- data.frame(sub_grupos=kc2$cluster, grupo2_no_scaled)
finan2_canal$sub_grupos <- as.factor(finan2_canal$sub_grupos)
head(finan2_canal)
```




```{r}
library(ggplot2)
for(i in 3:dim(finan2_canal)[2]){
  
  p <- ggplot(finan2_canal, aes(x = factor(sub_grupos), y=log(finan2_canal[, i]+1))) +  geom_boxplot()+
    labs(x = colnames(finan2_canal)[i]) +
    theme_gray() 
    
  
  print(p)
}

```

* Media de los grupos

```{r}
aggregate(.~sub_grupos,data=finan2_canal, FUN=mean)
```


* Mediana de los grupos

```{r}
aggregate(.~sub_grupos,data=finan1_canal, FUN=median)
```




* Agrupamiento por canales en el grupo 3 de financieras:

```{r}
dendrogram <- hclust(dist(log(grupo3_no_scaled[,2:31]+1), method = 'euclidean'), method = 'ward.D')

plot(dendrogram, xlab="", sub="", cex=0.9)
```


```{r}
require(cluster)

set.seed(93280)
kc3 <- kmeans(log(grupo3_no_scaled[,2:31]+1), 3 , nstart = 5, iter.max = 100)

clusplot(log(grupo3_no_scaled[,2:31]+1), kc3$cluster, color = TRUE, 
          shade = FALSE, labels = 4, lines = 1,
          col.p = kc3$cluster )
```


```{r}
table(kc3$cluster)
```


Creación del conjunto de datos con los grupos creados:


```{r}
finan3_canal <- data.frame(sub_grupos=kc3$cluster, grupo3_no_scaled)
finan3_canal$sub_grupos <- as.factor(finan3_canal$sub_grupos)
head(finan3_canal)
```




```{r}
library(ggplot2)
for(i in 3:dim(finan3_canal)[2]){
  
  p <- ggplot(finan3_canal, aes(x = factor(sub_grupos), y=log(finan3_canal[, i]+1))) +  geom_boxplot()+
    labs(x = colnames(finan3_canal)[i]) +
    theme_gray() 
    
  
  print(p)
}

```

* Media de los grupos

```{r}
aggregate(.~sub_grupos,data=finan3_canal, FUN=mean)
```


* Mediana de los grupos

```{r}
aggregate(.~sub_grupos,data=finan3_canal, FUN=median)
```



En general siempre el grupo 1 de los canales son los que más manejan dinero a través de los canales, el segundo los que manejan más o menos y por último el grupo 3 los que menos dinero manejan, este mismo comportamiento se ve en los tres subgrupos que se crearon en los tres grupos de las variables financieras


* Grupos finales 

```{r}
finan1_canal
```



```{r}
Grupo1 <- finan1_canal[finan1_canal$sub_grupos==1,]
Grupo2 <- finan1_canal[finan1_canal$sub_grupos==2,]
Grupo3 <- finan1_canal[finan1_canal$sub_grupos==3,]

Grupo4 <- finan2_canal[finan2_canal$sub_grupos==1,]
Grupo5 <- finan2_canal[finan2_canal$sub_grupos==2,]
Grupo6 <- finan2_canal[finan2_canal$sub_grupos==3,]

Grupo7 <- finan3_canal[finan3_canal$sub_grupos==1,]
Grupo8 <- finan3_canal[finan3_canal$sub_grupos==2,]
Grupo9 <- finan3_canal[finan3_canal$sub_grupos==3,]


Grupos <- c(rep(1, dim(Grupo1)[1]), rep(2, dim(Grupo2)[1]), rep(3, dim(Grupo3)[1]), rep(4, dim(Grupo4)[1]), rep(5, dim(Grupo5)[1]), rep(6, dim(Grupo6)[1]), rep(7, dim(Grupo7)[1]), rep(8, dim(Grupo8)[1]), rep(9, dim(Grupo9)[1]))


Datos_finales <- rbind(Grupo1, Grupo2, Grupo3, Grupo4, Grupo5, Grupo6, Grupo7, Grupo8, Grupo9)
Datos_finales <- data.frame(Grupos, Datos_finales)
Datos_finales
```



```{r}
library(ggplot2)
for(i in 4:dim(Datos_finales)[2]){
  
  p <- ggplot(Datos_finales, aes(x = factor(Grupos), y=log(Datos_finales[, i]+1))) +  geom_boxplot()+
    labs(x = colnames(Datos_finales)[i]) +
    theme_gray() 
    
  
  print(p)
}

```



#########################################################################################

## Resultados obtenidos donde se considerara 2 Grupos en la parte financiera
 
```{r}
require(cluster)

set.seed(93284)
kc <- kmeans(financiera, 2 , nstart = 5, iter.max = 100)

clusplot(financiera, kc$cluster, color = TRUE, 
          shade = FALSE, labels = 4, lines = 1,
          col.p = kc$cluster )
```

```{r}
fina_group <- df[, 31:46]

grupos <- as.factor(kc$cluster)

fina_group <- data.frame(grupos, fina_group)
```

```{r}
levels(fina_group$grupos)
```

```{r}
for(i in 2:dim(fina_group)[2]){
  
  p <- ggplot(fina_group, aes(x = fina_group[, i],y = ..prop..)) +
    geom_bar(fill = "#0c4c8a") +
    labs(x = colnames(fina_group)[i]) +
    theme_gray() +
    facet_grid(vars(), vars(grupos))
  
  print(p)
}

```


* Media de los grupos
```{r}
aggregate(.~grupos,data=fina_group, FUN=mean)
```
* Varianza de cada grupo
```{r}
aggregate(.~grupos,data=fina_group, FUN=var)
```
```{r}
aggregate(.~grupos,data=fina_group, FUN=median)
```
